<!DOCTYPE html>
<html class="light" lang="en">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>{{ $lp->title }}</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link
        href="https://fonts.googleapis.com/css2?family=Lexend:wght@100..900&amp;family=Noto+Sans:wght@100..900&amp;display=swap"
        rel="stylesheet" />
    <link
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap"
        rel="stylesheet" />
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        "primary": "#0d7ff2",
                        "accent-orange": "#ff6b00",
                        "background-light": "#f5f7f8",
                        "background-dark": "#101922",
                    },
                    fontFamily: {
                        "display": ["Lexend", "sans-serif"],
                        "sans": ["Noto Sans", "sans-serif"]
                    },
                    borderRadius: {
                        "DEFAULT": "0.25rem",
                        "lg": "0.5rem",
                        "xl": "0.75rem",
                        "full": "9999px"
                    },
                },
            },
        }
    </script>
    <style>
        body {
            font-family: 'Lexend', sans-serif;
        }

        .material-symbols-outlined {
            font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
        }
    </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-[#0d141c] dark:text-slate-50 overflow-x-hidden">
    <header
        class="sticky top-0 z-50 w-full bg-background-light/80 dark:bg-background-dark/80 backdrop-blur-md border-b border-solid border-[#e7edf4] dark:border-slate-800">
        <div class="max-w-[1200px] mx-auto px-4 lg:px-10 py-3 flex items-center justify-between whitespace-nowrap">
            <div class="flex items-center gap-3 text-[#0d141c] dark:text-white">
                <div class="size-8 text-primary">
                    <svg fill="none" viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                        <path clip-rule="evenodd"
                            d="M12.0799 24L4 19.2479L9.95537 8.75216L18.04 13.4961L18.0446 4H29.9554L29.96 13.4961L38.0446 8.75216L44 19.2479L35.92 24L44 28.7521L38.0446 39.2479L29.96 34.5039L29.9554 44H18.0446L18.04 34.5039L9.95537 39.2479L4 28.7521L12.0799 24Z"
                            fill="currentColor" fill-rule="evenodd"></path>
                    </svg>
                </div>
                <h2 class="text-xl font-black leading-tight tracking-[-0.015em] uppercase italic">{{ $lp->title }}</h2>
            </div>
            <div class="hidden md:flex flex-1 justify-center gap-8">
                <a class="text-sm font-medium hover:text-primary transition-colors" href="#">Info</a>
                <a class="text-sm font-medium hover:text-primary transition-colors" href="#">Percursos</a>
                <a class="text-sm font-medium hover:text-primary transition-colors" href="#">Cronograma</a>
            </div>
            <div class="flex items-center gap-4">
                <button
                    class="bg-primary hover:bg-primary/90 text-white px-5 py-2 rounded-lg text-sm font-bold transition-all shadow-lg shadow-primary/20">
                    Inscreva-se
                </button>
            </div>
        </div>
    </header>
    <section class="relative">
        <div class="w-full max-w-[1280px] mx-auto @container p-4 lg:p-10">
            <div class="flex min-h-[560px] flex-col gap-6 bg-cover bg-center bg-no-repeat rounded-xl items-center justify-center p-8 text-center"
                style='background-image: linear-gradient(rgba(16, 25, 34, 0.6) 0%, rgba(16, 25, 34, 0.8) 100%), url("{{ asset($content['hero']['bg_image'] ?? '') }}");'>
                <div class="flex flex-col gap-4 max-w-[700px]">
                    <span
                        class="inline-block self-center bg-accent-orange px-3 py-1 rounded-full text-xs font-bold text-white uppercase tracking-widest">{{ $content['hero']['date_text'] ?? '' }}</span>
                    <h1
                        class="text-white text-5xl font-black leading-tight tracking-[-0.033em] md:text-7xl italic uppercase">
                        {{ $content['hero']['title'] ?? $lp->title }}
                    </h1>
                    <p class="text-white/90 text-lg font-normal leading-relaxed md:text-xl">
                        {{ $content['hero']['subtitle'] ?? '' }}
                    </p>
                </div>
                <div class="flex flex-col sm:flex-row gap-4 mt-4">
                    <button
                        class="flex min-w-[180px] cursor-pointer items-center justify-center rounded-lg h-14 px-8 bg-primary text-white text-lg font-bold hover:scale-105 transition-transform shadow-xl shadow-primary/30">
                        {{ $content['hero']['cta_text'] ?? 'Inscreva-se Agora' }}
                    </button>
                </div>
            </div>
        </div>
    </section>

    @if(isset($content['about']))
        <section class="py-20 bg-white dark:bg-background-dark">
            <div class="max-w-[1200px] mx-auto px-6 lg:px-10">
                <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
                    <div class="relative">
                        <div class="aspect-[4/3] rounded-2xl overflow-hidden shadow-2xl">
                            <img alt="Sobre" class="w-full h-full object-cover"
                                src="{{ asset($content['about']['image'] ?? '') }}" />
                        </div>
                    </div>
                    <div class="flex flex-col gap-6">
                        <div>
                            <h2
                                class="text-[#0d141c] dark:text-white text-3xl md:text-5xl font-black leading-tight tracking-tight italic uppercase mt-2">
                                {{ $content['about']['title'] ?? '' }}
                            </h2>
                            <div class="h-1.5 w-20 bg-primary mt-4 rounded-full"></div>
                        </div>
                        <p class="text-slate-600 dark:text-slate-300 text-lg leading-relaxed">
                            {!! nl2br(e($content['about']['description'] ?? '')) !!}
                        </p>
                        <div class="grid grid-cols-2 gap-6 mt-4">
                            @foreach($content['about']['stats'] ?? [] as $stat)
                                <div class="flex flex-col">
                                    <span class="text-primary text-3xl font-black">{{ $stat['value'] }}</span>
                                    <span
                                        class="text-slate-500 dark:text-slate-400 font-bold uppercase text-xs tracking-widest">{{ $stat['label'] }}</span>
                                </div>
                            @endforeach
                        </div>
                    </div>
                </div>
            </div>
        </section>
    @endif

    @if(isset($content['prices']['items']))
        <section class="py-20 bg-white dark:bg-background-dark">
            <div class="max-w-[1200px] mx-auto px-6">
                <div class="flex flex-col items-center text-center mb-12">
                    <h2
                        class="text-[#0d141c] dark:text-white text-3xl md:text-4xl font-black leading-tight tracking-tight italic uppercase">
                        Escolha sua Distância</h2>
                    <div class="h-1.5 w-24 bg-primary mt-4 rounded-full"></div>
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-{{ count($content['prices']['items']) }} gap-6">
                    @foreach($content['prices']['items'] as $item)
                        <div
                            class="p-8 rounded-xl border {{ $item['is_popular'] ? 'border-2 border-primary relative shadow-xl shadow-primary/10 bg-white dark:bg-background-dark' : 'bg-slate-50 dark:bg-slate-900/40 border-[#e7edf4] dark:border-slate-800' }} flex flex-col hover:border-primary transition-all group">
                            @if($item['is_popular'])
                                <div
                                    class="absolute -top-4 left-1/2 -translate-x-1/2 bg-accent-orange text-white text-[10px] font-black uppercase px-3 py-1 rounded-full tracking-tighter">
                                    Mais Popular</div>
                            @endif
                            <span class="text-primary font-bold text-5xl mb-2">{{ $item['distance'] }}</span>
                            <h3 class="text-xl font-bold mb-4 dark:text-white">{{ $item['title'] }}</h3>
                            <p class="text-slate-500 dark:text-slate-400 text-sm mb-6 flex-grow">{{ $item['description'] }}</p>
                            <div class="flex items-baseline gap-1 mb-6">
                                <span class="text-3xl font-black dark:text-white">{{ $item['price'] }}</span>
                            </div>
                            <button
                                class="w-full py-3 rounded-lg {{ $item['is_popular'] ? 'bg-primary text-white' : 'border-2 border-primary text-primary' }} font-bold hover:scale-[1.02] transition-all">Inscrever</button>
                        </div>
                    @endforeach
                </div>
            </div>
        </section>
    @endif

    <footer class="bg-slate-950 text-white py-16">
        <div class="max-w-[1200px] mx-auto px-6 text-center">
            <p class="text-slate-500 text-xs">
                © {{ date('Y') }} {{ $lp->title }}. Todos os direitos reservados.
            </p>
        </div>
    </footer>
</body>

</html>